CREATE TABLE testTable(
    id varchar,
    city varchar,
    temperature DOUBLE,
    feelsLike DOUBLE,
    temperatureMin DOUBLE,
    temperatureMax DOUBLE,
    pressure DOUBLE,
    humidity DOUBLE,
    PRIMARY KEY(id)
);